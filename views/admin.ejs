<%- include("partials/head", { title: "Admin — " + siteName }) %>
<%- include("partials/header") %>

<main class="wrap">
  <h1>Menaxho regjistrimet</h1>
  <p><%= listings.length %> regjistrime gjithsej.</p>

  <table class="admin-table">
    <thead>
      <tr>
        <th>Qyteti</th>
        <th>Profesioni</th>
        <th>Emri</th>
        <th>Telefoni</th>
        <th>Shënim</th>
        <th>Data</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% listings.forEach(function(l) {
        var cat = categories.find(function(c) { return c.slug === l.category; });
        var cty = cities.find(function(c) { return c.slug === l.city; });
      %>
        <tr>
          <td><%= cty ? cty.label : l.city %></td>
          <td><%= cat ? cat.label : l.category %></td>
          <td><%= l.name %></td>
          <td><%= l.note %></td>
          <td><%= new Date(l.createdAt).toLocaleDateString('sq') %></td>
          <td>
            <form method="post" action="/admin/fshij/<%= l.id %>" onsubmit="return confirm('Fshi këtë regjistrim?');">
              <button type="submit" class="btn secondary">Fshi</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</main>

<%- include("partials/footer") %>
